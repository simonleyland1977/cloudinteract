export const metadata = {
    title: 'AWS Partner Portal | CloudInteract',
    description: 'Exclusive resources for AWS employees on Amazon Connect and Agentic AI solutions.',
};

export default function AWSPortalLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
